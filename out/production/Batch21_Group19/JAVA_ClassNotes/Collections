

** Collections doesnt accept primitives
** Collections uses bulk Operations

Example of Collection methods :

* containsAll (CollectionType);
* addAll(CollectionType);
* removeAll(CollectionType);
* retainAll(CollectionType);
* removeIf (Predicate )  =>      Predicate<DataType>  name= p-> boolean expression
* isDigits
* isLetter